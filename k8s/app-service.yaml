apiVersion: v1
kind: Service
metadata:
  name: hotelease-app-service
  labels:
    app: hotelease-app
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
      protocol: TCP
  selector:
    app: hotelease-app
